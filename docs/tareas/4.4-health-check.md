# Tarea 4.4 — Health check

> Fase 4 — Operabilidad · 🟡 Ítem 11 del plan · Depende de: fase 2 · Reglas: [README.md](README.md)

## Contexto

Sin endpoint de salud, ni el reverse proxy ni el monitoreo (ni el `depends_on` del compose de la tarea 4.6) pueden saber si la app está viva y conectada a la BD.

## Archivos

- Nuevo `src/modules/health/health.module.ts` y `health.controller.ts`
- `src/app.module.ts`

## Pasos

1. `pnpm add @nestjs/terminus`.
2. `GET /api/v1/health` con `@Public()` (definido en la tarea 2.2): `HealthCheckService` + `TypeOrmHealthIndicator.pingCheck('database')`.
3. No exponer información sensible en la respuesta (versions, hosts) — el formato estándar de terminus es suficiente.

## Criterio de aceptación

- [ ] Con BD arriba: `GET /api/v1/health` → 200 `{ status: 'ok', ... }` sin token.
- [ ] Con BD caída (`docker compose stop db`): → 503.
- [ ] Commit: `feat(health): public health endpoint with database ping`.

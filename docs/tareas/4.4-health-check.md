# Tarea 4.4 — Health check

> Fase 4 — Operabilidad · 🟡 Ítem 11 del plan · Depende de: fase 2 · Reglas: [README.md](README.md)

## Contexto

Sin endpoint de salud, ni el reverse proxy ni el monitoreo (ni el `depends_on` del compose de la tarea 4.6) pueden saber si la app está viva y conectada a la BD.

## Archivos

- Nuevo `src/modules/health/health.module.ts` y `health.controller.ts`
- `src/app.module.ts`

## Pasos

1. `pnpm add @nestjs/terminus`.
2. `GET /api/v1/health` con `@Public()` (definido en la tarea 2.2): `HealthCheckService` + `TypeOrmHealthIndicator.pingCheck('database')`.
3. No exponer información sensible en la respuesta (versions, hosts) — el formato estándar de terminus es suficiente.

## Criterio de aceptación

- [x] Con BD arriba: `GET /api/v1/health` → 200 `{ status: 'ok', ... }` sin token.
- [x] Con BD caída (`docker compose stop db`): → 503.
- [x] Commit: `feat(health): public health endpoint with database ping`.

## Registro de verificación

### Pruebas E2E automatizadas
Se agregó un test en [app.e2e-spec.ts](file:///e:/proyectos/Control_produccion_back/test/app.e2e-spec.ts) para verificar que `GET /api/v1/health` responda 200 OK y que retorne el estado `'ok'` indicando que la base de datos está activa (`'up'`), sin requerir cabeceras de autorización de forma segura (`@Public()`).

Resultados:
- **Build de producción:** `pnpm build` -> Exitosa.
- **Tests unitarios:** `pnpm test` -> 41/41 aprobados.
- **Tests de integración:** `pnpm test:e2e` -> 15/15 aprobados.
- **Linter:** `pnpm lint` -> 0 errores.
- **Verificación manual:** 
  - Con BD arriba: `/api/v1/health` responde 200 OK con el payload estructural de Terminus indicando `database: { status: 'up' }`.
  - Con BD abajo (`docker compose stop db`): `/api/v1/health` retorna HTTP 503 Service Unavailable y `database: { status: 'down' }`.

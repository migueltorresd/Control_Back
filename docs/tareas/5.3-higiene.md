# Tarea 5.3 — Higiene de frontend y repositorios

> Fase 5 — Pulido · 🟢 Ítems 17 y 18 del plan · Depende de: nada (puede hacerse en paralelo a la fase 5) · Reglas: [README.md](README.md)

## Contexto

Cierres menores en ambos proyectos: lint del frontend, metadatos de paquetes, auditoría de dependencias y documentación de arquitectura para futuros mantenedores.

## Pasos

### Frontend (`e:\proyectos\control-produccion`)

1. Añadir ESLint con config para React (flat config, `eslint-plugin-react-hooks` incluido) + script `"lint"` en `package.json`. Corregir lo que destape.
2. Verificar que no exista `dangerouslySetInnerHTML` ni construcción de HTML por strings (regla 5 del README).
3. Revisar el manejo de errores de `request()` en `api.js`: ya debería parsear el JSON del backend (tarea 2.4) — confirmar que las vistas muestran mensajes amigables, no objetos crudos.

### Ambos repos

4. `package.json`: completar `description` y `author`; revisar la licencia (`UNLICENSED` — confirmar que es lo acordado contractualmente con el cliente).
5. `pnpm audit` y resolver vulnerabilidades altas/críticas (las menores: registrarlas y decidir).
6. Verificar la versión declarada de `typeorm` en `package.json` del backend (`^1.0.0`) — confirmar que es la línea correcta y estable que está instalada en el lockfile; alinear el rango si no.

### Documentación

7. README del backend: sección breve de arquitectura — los 6+1 módulos, el flujo de negocio (vale → producción por etapas → aprobación → pago → venta), la máquina de estados con su diagrama en texto, y enlaces a `docs/PLAN-MEJORAS-CALIDAD.md`.
8. Verificar que el README ya tiene las secciones de Despliegue y Backups (tarea 4.6) y el flujo de migraciones (tarea 1.2).

## Criterio de aceptación

- [ ] `pnpm lint` verde en ambos proyectos.
- [ ] `pnpm audit` sin altas/críticas pendientes sin justificar, en ambos.
- [ ] README con arquitectura, despliegue, backups y migraciones.
- [ ] Commit: `chore: frontend eslint, package metadata, audit and architecture docs`.
